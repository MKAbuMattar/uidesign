# uidesign

This extension provides the **ui-design** skill.

Read `skills/ui-design/SKILL.md` and follow it. That file holds the method, the clarify-first
questions, the output contract, and the reference map; load only the reference a task needs from
`skills/ui-design/references/`.
