# Assets

Empty until roadmap increment 2. Planned: `logo.svg` (horizontal lockup), `icon.svg` (square
mark), favicons, and per-command glyphs for the seven commands.

Self-contained SVG only: no external fonts, no remote references. `.codex-plugin/plugin.json`
gets its `logo` key back once `icon.svg` lands.
